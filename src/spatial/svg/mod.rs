//! Spatially variable gene (SVG) detection methods.
//!
//! Currently exposes Moran's I via [`morans_i`]. SPARK-X is planned (see
//! `sparkx` module placeholder).

pub mod morans_i;
pub mod shared;
// pub mod sparkx;  // phase 2
